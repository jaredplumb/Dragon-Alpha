# Runtime Overrides (Approved)

Place only manually approved WAV runtime overrides here.
These files should be copied into `Resources/Sounds` by your local asset-import workflow.

Do not bulk-copy recovered candidates into this folder.
Use `Assets/Sounds/RecoveredOverrides` for unreviewed recovery output.

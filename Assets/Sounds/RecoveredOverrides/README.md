# Recovered Overrides (Candidates)

This folder is for recovered audio candidates staged by local recovery tooling.
Files here are not applied to runtime automatically.
Byte-identical duplicates versus canonical legacy runtime WAVs are pruned during cleanup.

Promote only reviewed files to `Assets/Sounds/RuntimeOverrides`.

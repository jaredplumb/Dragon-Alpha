# Deprecated Archive Staging

This folder is intentionally minimal in the public-facing tree.

- `RecoveryScratch/` remains as the canonical local recovery-scratch path expected by validators.
- `RecoveryNotes/` stores historical one-off asset recovery/import notes that are not active manifests.
- Large legacy bundle snapshots and old extraction debris were intentionally omitted from this public repository during cleanup.

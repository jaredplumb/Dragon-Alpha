# Recovery Notes (Historical)

This folder stores historical one-off recovery/import notes moved out of `Assets/Docs/`
so that `Assets/Docs/` remains focused on current generated manifests.

These notes are retained for provenance only and are not active build/runtime inputs.

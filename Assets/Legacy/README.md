# Legacy

Historical archive content retained for provenance and recovery work.

Includes:
- legacy code-era trees (`Source`, `Headers`, plugin app bundles)
- disk-image recovery datasets (`Recovered`)
- legacy interface resources (`Resources/*.nib`, `*.xib`)

Pre-push cleanup notes:
- oversized DragonArcadia zip-era payloads were omitted from this public repository.
- heavy recovered DMG carved/curated/manual-mount payloads were omitted from this public repository
  while keeping canonical legacy plugin data in `Assets/Legacy/Data/Areas/` and lightweight
  recovery manifests in `Assets/Legacy/Recovered/`.

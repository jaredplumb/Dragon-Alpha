# DragonAlphaDMG-ManualMounted (Pruned)

This directory was reduced for pre-push cleanup.

Heavy manual-mount payloads were intentionally omitted from this public repository.

Canonical legacy plugin/map data remains under:
- `Assets/Legacy/Data/Areas/`

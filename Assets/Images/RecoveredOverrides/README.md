# Recovered Overrides (Candidates)

This folder is for recovered image candidates staged by local recovery tooling.
Files here are not applied to runtime automatically.
Byte-identical duplicates versus canonical legacy image sets are pruned during cleanup.

Promote only reviewed files to `Assets/Images/RuntimeOverrides`.

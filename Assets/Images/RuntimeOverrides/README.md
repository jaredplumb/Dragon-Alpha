# Runtime Overrides (Approved)

Place only manually approved PNG runtime overrides here.
These files should be copied into `Resources/Images` by your local asset-import workflow.

Do not bulk-copy recovered candidates into this folder.
Use `Assets/Images/RecoveredOverrides` for unreviewed recovery output.
